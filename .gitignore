# .NET build output
bin/
obj/

# Runtime-generated, machine-specific — not part of the source
app/MajesticAssistant/cache/
app/MajesticAssistant/settings.json

# Downloaded manually (multi-hundred-MB GGML model for voice input) — see README.
# Only the model file itself is ignored, not the folder, so it still exists after a fresh clone.
app/MajesticAssistant/whisper/*.bin

# IDE
.vs/
*.user
